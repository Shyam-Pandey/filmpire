import { makeStyles } from "@mui/styles";

export default makeStyles((theme) => ({
    characterImage: {
        maxWidth: '75%',
        borderRadius: '20px',
        boxShadow: '0.5em 0.5em 1em',
        objectFit: 'cover',
    },
}));